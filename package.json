{
  "name": "@silverorange/ambiguous-class-name-detector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check-if-pretty": "prettier --list-different '{src,tests}/**/*.php'",
    "make-pretty": "prettier --write '{src,tests}/**/*.php'"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.11.2",
    "prettier": "1.18.2"
  }
}
